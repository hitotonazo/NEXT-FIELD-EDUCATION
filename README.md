# IKUSHINSHA ARG site01

静的HTML/CSS/JSで構成したスマホ向けWeb探索型ARGサイトです。

## デプロイ
- Framework preset: None
- Build command: なし
- Build output directory: site01

## 共有ボタンの差し替え
初期状態では `js/script.js` 内の `DEFAULT_SHARE_URL` にダミーURLを設定しています。
公開後に実際の元ツイートURLへ差し替えてください。
